# carBill — AI 기반 통행료 정리 에이전트 가이드

> 이 문서는 어떤 AI 모델이 와도 동일한 품질로 처리할 수 있도록 작성된 실행 규칙이다.
> AI 에이전트는 이 문서를 가이드로 삼아 아래 프로세스를 수행한다.

---

## 1. 프로젝트 개요

사용자의 차량 통행료(하이패스) CSV를 받아, 각 거래를 **여정(trip)** 단위로 묶고
사용자의 주 동선 데이터를 참고해 **추정 출발지/도착지**를 부여한 뒤
정리된 CSV로 저장한다. 원본의 일자·통행료는 절대 손대지 않는다.

- 처리 주체: AI 에이전트 (스크립트·도구 사용 가능, 판단은 이 문서 규칙에 따름)
- 원칙: **원본 불변** + **추정은 추정으로 명시** + **모르면 사용자에게 질문**

---

## 2. 파일 구조

```
prj/carBill/
├── agents.md                  # 이 문서 (AI 실행 규칙)
├── viewer.html                # 결과 CSV 뷰어 (합계 자동 계산, 샘플 자동로드 지원)
├── data/
│   ├── sample/                # 공개용 가상 데모 데이터 (sample-toll/route/result.csv)
│   └── private/               # 로컬 전용 실제 데이터 (git ignore, 공개 금지)
│       ├── toll/              # 사용자가 올린 원본 (예: private/toll/2026-8 통행료.csv)
│       ├── route-data.csv      # 사용자 동선 지식 베이스 (핵심, 지속 누적)
│       └── results/            # 처리 결과 (상세, 불변 로우 포함)
```

---

## 3. 입력 데이터 규격 (하이패스 통행료 CSV)

### 3-1. 구조
- 파일 상단에 한글 설명 블록(기간, 건수, 합계 등)이 여러 줄 있다.
- **실제 헤더는 `번호`로 시작하는 행**이다. 그 행을 헤더로 취급한다.
- 헤더 다음이 데이터 행이며, `번호`는 역순(큰 번호부터)이다.

### 3-2. 핵심 컬럼
| 컬럼 | 의미 | 사용 |
|---|---|---|
| 거래일시 | 여행이 완료된 시각 (`YYYY/MM/DD HH:MM:SS`) | 일자·주차·정렬 기준 |
| 입구 / 출구 | 진입/진출 요금소(게이트) 이름 | 여정 그룹핑·동선 매칭 |
| 청구금액 | 실제 청구된 금액 (`"9,200"` 형태, 콤마 포함 쿼트) | 통행료 (불변) |
| 납부할통행료 | 청구금액과 항상 동일 | 검증용 |
| 비고 | 할인/할증/정상 여부 | 참고 |

> ⚠️ 금액 컬럼은 콤마가 포함된 쿼트 문자열이다. `awk -F','` 같은 단순 분할로 파싱하면
> 컬럼이 밀린다. **정식 CSV 파서**(인용부호·콤마 처리)를 반드시 사용한다.

### 3-3. 특이 거래 유형
- **출구전용 거래**: `입구`가 `-` 또는 입구일시가 `/ / 00:00:00`인 거래.
  요금소 없이 진입(로컬 도로 합류 등)해 출구 요금소에서만 찍힌 거래.
  **단독으로는 여정이 될 수 없다.** 인접 거래와 병합해서 해석한다.
- **입구일시가 불명(`/ / 00:00:00`)**: 동일하게 인접 거래와 병합한다.

---

## 4. 불변 데이터 원칙 (가장 중요)

다음 값은 **원본 그대로 복사**하며 절대 수정·추정·재계산하지 않는다.

| 항목 | 근거 |
|---|---|
| 몇월몇주차 | 거래일시의 날짜 기준, **1일~7일 고정** 주차 (8/1~8/7=1주차, 8/8~8/14=2주차, …) |
| 일자 | 거래일시의 `YYYY-MM-DD` 부분 |
| 통행료 | 청구금액 (원본 문자열 숫자 그대로) |
| 입구/출구 | 원본 그대로 |

**검증**: 결과 CSV의 주차별·일자별 통행료 합계 = 원본의 전체 합계(파일 상단 `거래금액 합계`)와
반드시 일치해야 한다. 불일치가 발견되면 숫자를 억지로 고치지 말고 **사용자에게 보고**한다.

AI가 추정할 수 있는 것은 오직 **여정 그룹핑, 동선명, 추정출발지/추정도착지**뿐이다.
모든 추정 컬럼은 결과 CSV에 `추정`이라는 단어로 명시한다.

---

## 5. 여정(trip) 그룹핑 규칙

여정 = "한 번의 연속 이동". 하나의 긴 경로는 여러 개의 통행료 거래로 쪼개져 찍히므로
(예: 부산→서울 = 요금소마다 5건) 이를 하나의 여정으로 묶는다.

### 판정 기준 (거래일시 오름차순으로)
1. **게이트 체인 연속성**:
   - 다음 거래의 `입구` == 이전 거래의 `출구` (예: 게이트1→**게이트2**, **게이트2**→게이트3) → 같은 여정
   - 다음 거래 `입구`가 `-`(출구전용) → 이전 거래와 같은 여정으로 흡수
   - 게이트명이 앞/뒤 접미사만 다른 경우(포승상/포승상2 등) → 같은 여정으로 취급
2. **시간 연속성**:
   - 거래 간 실제 공백(대기·방문)이 없고 이어지는 주행이면 같은 여정.
   - 단, 거래일시 간격은 **주행 시간을 포함**하므로(장거리 구간 = 3시간대),
     "게이트 체인이 방향성 있게 이어지면" 긴 간격도 같은 여정으로 묶는다.
   - 자정(00시)을 넘기는 대기 공백은 여정 분리 기준.
3. **목적지 체류 중단**:
   - 목적지에서 머문 뒤(예: 80분 뒤 역방향 재진입) 돌아오는 거래는 **새 여정**으로 분리.
     왕복 여정은 가는 길과 오는 길을 각각 별도 여정으로 분리한다.
4. **예외 처리**:
   - 같은 날짜에 여정이 여러 개면 여정ID를 구분한다.
   - 며칠에 걸친 장거리 여행은 당일 여정으로 나누되 **여행ID로 결속**한다.

### 여정ID 부여
- `J01, J02, ...` — 월 내 순번.
- 여행ID: 다일 여행일 때만 `TRIP-{이름}` 형태로 부여 (예: `TRIP-여행지명`).

---

## 6. 동선 데이터 규격 (`data/private/route-data.csv`)

사용자의 주 동선 지식 베이스. 초기에는 사용자가 직접 제공하고, 이후 AI가 새로 확정된
동선을 **추가**한다. (기존 행은 수정하지 않는다 — 같은 동선의 신규 패턴은 새 행으로 추가)

| 컬럼 | 설명 | 예 |
|---|---|---|
| 동선ID | 고유 키 | `DT-AM` |
| 동선명 | 사용자가 이해하는 이름 | `출퇴근(아침)` |
| 추정출발지 | 출발 장소 (사용자 용어) | `집` |
| 추정도착지 | 도착 장소 (사용자 용어) | `회사` |
| 경로패턴 | 게이트를 이동 순서대로 `→`로 연결 (출구전용은 게이트명만) | `게이트A→게이트B→게이트C` |
| 시간대 | 전형적 시간대 | `07~09`, `저녁 19~21`, `주말`, `목요일 저녁` |
| 방향 | `왕복`/`편도` | `왕복` |
| 비고 | 특이사항 | `네비 우회 시`, `게이트 패턴 미확정` |

> 같은 동선명에 서로 다른 경로패턴(다른 진입점 등)이 있으면 별도 행으로 추가한다.

> ⚠️ **개인정보 보호**: 실제 개인 위치(집 주소, 회사, 여친집, 본가 등)는
> 이 문서 및 README 등 공개 문서에 절대 기록하지 않는다.
> 추정출발지/추정도착지의 실제 값은 `data/private/` 안에서만 관리하며,
> 공개 예시에는 반드시 가상 장소(집/회사 등)를 사용한다.

---

## 7. 동선 매칭 및 추정

여정이 확정되면 다음 우선순위로 추정 출발지/도착지를 부여한다.

1. **경로패턴 정확 일치**: 여정의 게이트 시퀀스가 동선 데이터의 `경로패턴`과 일치하면
   해당 동선의 추정출발지/추정도착지를 그대로 사용.
2. **부분 일치 + 시간대**: 패턴이 부분적으로 겹치고 시간대(아침/저녁/주말)가 일치하면
   가장 유사한 동선으로 추정. (예: 출퇴근에 쓰는 게이트 체인의 일부만 찍힌 경우 → 출퇴근으로 추정)
3. **미매칭**: 아래 질문 프로토콜로 사용자에게 확인.

추정값은 `동선명` + `추정출발지` + `추정도착지` 컬럼으로 출력하며,
사용자가 나중에 뷰어에서 교정할 수 있음을 명시한다.

---

## 8. 질문 프로토콜

미매칭 여정(또는 애매한 그룹핑)을 발견하면:

1. 해당 여정의 게이트 체인과 날짜·시간대를 보여준다.
2. "이 경로는 어디서 어디로 가는 건가요?" 라고 질문한다. (보기 제시 가능)
3. 답변을 받으면 **동선 데이터에 새 행으로 추가**한다. (기존 행 수정 금지)
4. 추가 후 매칭을 재실행하여 결과를 갱신한다.

첫 실행(동선 데이터가 비어 있을 때)에는 월 데이터의 주요 클러스터를 먼저 보여주고
한 번에 질문하여 초기 베이스를 구축한다.

---

## 9. 출력 CSV 규격 (`data/private/results/{YYYY-M}-통행료-정리.csv`)

- 인코딩: UTF-8 (BOM 포함) — 엑셀 호환
- **로우 단위: 원본 거래 1건당 1행** (불변 원칙). 여정 정보는 컬럼으로 추가.
- 금액은 숫자 그대로 (콤마 없이 정수).

| 컬럼 | 내용 | 출처 |
|---|---|---|
| 몇월몇주차 | `8월 1주차` | 계산 (불변) |
| 일자 | `2026-08-01` | 거래일시 (불변) |
| 거래시각 | `HH:MM:SS` | 거래일시 (불변) |
| 여정ID | `J01` | AI 그룹핑 (추정) |
| 여행ID | `TRIP-여행지명` (없으면 공백) | AI 그룹핑 (추정) |
| 동선명 | `출퇴근(아침)` | AI 추정 |
| 추정출발지 | `집` | AI 추정 |
| 추정도착지 | `회사` | AI 추정 |
| 입구 | 원본 그대로 | 불변 |
| 출구 | 원본 그대로 | 불변 |
| 통행료 | 청구금액 정수 | 불변 |
| 원본번호 | 원본 `번호` | 참조 |
| 비고 | 원본 `비고` | 참조 |

> 합계(주차별/일자별/경로별)는 별도 파일로 만들지 않는다.
> viewer.html이 이 상세 CSV를 읽어 **JS로 자동 계산**한다.
> AI는 대신 "검증용"으로 일자별·주차별 합계가 원본 합계와 일치하는지 확인만 한다.

---

## 10. 금지사항

- ❌ 통행료·일자·주차 숫자 조작/반올림/재계산 금지 (불변 원칙)
- ❌ 존재하지 않는 거래·여정 창작 금지
- ❌ 추정출발지/도착지를 확정값처럼 표시 금지 (항상 `추정` 컬럼으로)
- ❌ 기존 동선 데이터 행 수정 금지 (추가만 가능)
- ❌ 합계 불일치 시 숫자 억지 맞추기 금지 → **사용자 보고**
- ❌ 사용자 질문 없이 미확인 경로 임의 라벨링 금지
- ❌ 개인 위치(집·회사·여친집·본가 주소 등)를 공개 문서·예시·README에 노출 금지
  (항상 가상 혹은 일반화된 표현만 사용, 실제 값은 `data/private/`에만 보관)

---

## 11. 실행 체크리스트

1. `data/private/toll/{YYYY-M} 통행료.csv` 존재 확인
2. 정식 CSV 파서로 파싱, 헤더(`번호` 행) 탐지
3. 거래일시 기준 오름차순 정렬
4. 여정 그룹핑 (§5) → 여정ID 부여
5. 동선 매칭 (§7) → 미매칭은 질문 (§8)
6. 답변을 동선 데이터에 추가 후 재매칭
7. 결과 CSV 생성 (§9)
8. **검증**: 주차별/일자별 합계 vs 원본 합계 일치 확인 (§4)
9. 완료 보고: 총 거래 수, 총 통행료, 여정 수, 미확정 항목이 있으면 함께 보고